// Header.js
import React from 'react';
import Navigation from './Navigation';

function Header() {
  return (
    <header id="header">
      <nav>
        <Navigation /> {Navigation}
      </nav>
    </header>
  );
}

export default Header;
